- name: Create the custom web root folder
  file: path=/var/html state=directory mode=0700

- name: Retrieve IP address
  shell: hostname -i
  register: ipaddress

- debug: var=ipaddress

- name: Deploy custom web page
  template: src=index.html.j2 dest=/var/html/index.html
